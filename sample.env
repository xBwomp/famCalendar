# Google OAuth2 Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=ID_Here
GOOGLE_CLIENT_SECRET=SECRET_HERE
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# Session Configuration
# REQUIRED: Used to sign session cookies. Must be a secure random string (min 32 chars).
# Generate with: openssl rand -hex 32
SESSION_SECRET=SESSION_SECRET_HERE_CHANGE_ME_MINIMUM_32_CHARS

# Encryption Configuration
# REQUIRED: Used to encrypt OAuth tokens stored in the database (min 32 chars).
# Generate with: openssl rand -hex 32
# IMPORTANT: Keep this secret! If lost, you'll need to re-authenticate with Google.
ENCRYPTION_KEY=ENCRYPTION_KEY_HERE_CHANGE_ME_MINIMUM_32_CHARS

# CORS Configuration
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# Add your frontend URLs here (no trailing slashes)
CORS_ORIGINS=http://localhost:5173,http://localhost:3001

# Server Configuration
PORT=3001
NODE_ENV=development
HOST=localhost

# Database Configuration
DB_PATH=./data/calendar.db

# Sync Configuration
SYNC_INTERVAL_MINUTES=15
MAX_EVENTS_DAYS=30
