{
  "name": "family-calendar",
  "version": "1.0.0",
  "description": "Local Calendar Dashboard for Raspberry Pi",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "npm run start --workspace=backend"
  },
  "keywords": [
    "calendar",
    "raspberry-pi",
    "google-calendar",
    "dashboard"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.2",
    "googleapis": "^160.0.0"
  }
}
